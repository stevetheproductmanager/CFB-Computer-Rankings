{
  "name": "cfbd-rankings-tool",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c yellow,cyan \"npm:server\" \"npm:client\"",
    "server": "npm --workspace server run dev",
    "client": "npm --workspace client run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}